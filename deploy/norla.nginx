server {
    listen 80;
    server_name your-domain.com www.your-domain.com;   # <-- CHANGE THIS

    # Max upload size (matches the 15MB app limit)
    client_max_body_size 20M;

    # Static files served directly by nginx (faster)
    location /_next/static/ {
        alias /var/www/norla/.next/static/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location /public/ {
        alias /var/www/norla/public/;
        expires 7d;
    }

    # Everything else goes to Next.js
    location / {
        proxy_pass         http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
